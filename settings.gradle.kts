pluginManagement {
    repositories {
        mavenLocal()
        maven("https://maven.teamvoided.org/releases") {
            name = "TeamVoided"
        }
    }
}

rootProject.name = "hytale-plugin-template"